apiVersion: v1
kind: Service
metadata: 
    name: result
    labels: 
        name: result-services
        app: demo-voting-app
spec:
    type: LoadBalancer
    ports:
      - port: 80
        targetport: 80
    selector:
        name: result-app-pod
        app: demo-Result-app
            
